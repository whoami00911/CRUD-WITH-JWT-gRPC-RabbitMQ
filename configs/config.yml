server:
  port: 8080

token:
  token_ttl: 1500m
  token_key: petersburgSalt
  refreshToken_ttl: 1440h

db_tables:
  crud: AbuseEntity
  auth: users
  tokens: refresh_tokens

hashphrase: somesalte

grpc:
  ip: localhost
  port: 8889